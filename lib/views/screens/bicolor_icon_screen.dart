import 'package:flutter/material.dart';

class BicolorIconScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("BicolorIconScreen")
      ),
    );
  }
}